import { FormContainer } from "../../components/Containers/FormContainer";
import { ResetForm } from "../../components/Forms/Forms";
const ResetPassword = () => {
  return (
    <FormContainer>
      <ResetForm />
    </FormContainer>
  );
};

export default ResetPassword;
